#!/bin/bash
# backup files

uuid=0AC20AE631DA7219
if lsblk -f | grep -wq $uuid; then
    sudo rsync -aAXv --delete --dry-run\
         --exclude={'go', '.cache'}\
         /home/bennett /backups/linux
else
    echo "please connect backup drive"
fi
