#!/bin/sh
#
# filesystem left

notify-send "starting disk summary"
pgrep -x dunst >/dev/null && notify-send "💽 Disk space" "$(df -h / --output=target,used,size && echo -e '\\nFolders' && du -t 1M -sh .[!.]* * | sort -hr)"
