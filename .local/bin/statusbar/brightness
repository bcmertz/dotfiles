#!/bin/sh
#
# brightness

brightness=$(xbacklight -get | cut -d '.' -f 1)
icon="🔆"

if [ "$1" == "-n" ]; then
    printf "%s%s%%\\n" "$brightness"
else
    printf "  %s%s%%\\n" "$icon" "$brightness"
fi
