#!/bin/sh
#
# exit menu

options="matrix
lock
logout
reboot
shutdown
exit i3
bonsai"

chosen=$(printf "$options" | dmenu -c -i -l 20 -p '👋:' -bw 3)
[ "$chosen" = "" ] && exit

case $chosen in
    bonsai)
        fullscreen -D "cbonsai -lt 0.15 -i -w 2.0 -L 60"
    ;;

    matrix)
        fullscreen "cmatrix"
    ;;

    "exit i3")
        i3-msg exit
        ;;

    logout)
        pkill -9 -u leah
        ;;

    shutdown)
        shutdown now
        ;;

    lock)
        screenlock
        ;;

    reboot)
        shutdown -r now
    ;;

esac
